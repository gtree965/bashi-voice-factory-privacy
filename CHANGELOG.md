# 更新日志

本项目的重要变更按版本记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

说明：当前 GitHub 仓库创建于 2026-07-31；v0.1.0–v0.1.2 的 GitHub Release 条目于当日补建，因此页面日期不作为这三个版本的版本日期。下表采用 Git 历史与项目版本记录中保存的日期。

| 版本 | 版本日期 |
|---|---|
| v0.1.5 | 待发布 |
| v0.1.4 | 2026-09-08 |
| v0.1.3 | 2026-08-03 |
| v0.1.2 | 2026-06-15 |
| v0.1.1 | 2026-06-02 |
| v0.1.0 | 2026-05-26 |

## v0.1.5 — 待发布

### 新增

- 发行包内首次附带完整第三方许可材料：`THIRD_PARTY.md`、`THIRD_PARTY_GAPS.md` 与 `licenses/` 整目录，共 34 份（含上游 Qwen3-TTS-GGUF 转换运行时的 MIT 原文）。
- 构建期许可材料落包门禁：每次构建都将包内许可原文与仓库索引逐文件比对，不一致即拒绝出包。

### 变更

- 界面页脚许可表述修正：改为「项目代码 MIT；模型与第三方组件遵循各自许可条款」，不再对整个发行包作统一授权承诺。
- 导出文件名修正。
- 快速开始的启动说明修正。

## v0.1.4 — 2026-09-08

### 新增

- 界面加载时异步预热完整 TTS 合成链路；预热期间的早到请求会等待，而不是直接失败。
- 构建期 PDF 手册门禁（`Assert-PdfUserGuideSafe`）：手册含任何非 HTTPS 链接即拒绝出包。
- Gitee 镜像：源码与下载新增 Gitee 入口，方便中国大陆用户访问。

### 修复

- 修复随包 PDF 使用手册中的失效内链（此前指向构建机器本地目录）。

## v0.1.3 — 2026-08-03

### 新增

- 中文 STT 静态纠错层：`zh_confusion.py` + 可编辑词表 `data/zh_confusion.tsv`，高风险替换默认关闭。
- 流式合成控制：停止不丢已完成片段；首段默认自动播放；长文分组预览，并保留可拖动、可下载的完整 MP3。
- 应用日志 `app.log`（轮转）；启动器与原生 stderr 保留在 `launch_log.txt`。

### 变更

- 移除 Paraformer Chinese Large 路径；FireRedASR 不再作为产品候选；Speaker ID 界面默认隐藏。
- STT 上传大小上限（默认 2 GB）与音频转换文件名校验收紧。

### 修复

- `stt_jobs` 状态加锁；前端 SSE 跨 chunk 缓冲，避免 JSON 分片丢失或误解析。
- 启动器依赖门禁：健康环境跳过 pip 安装，镜像缺包时回退上游 PyPI。
- 修正 vendored llama.cpp/ggml 的日志级别映射。

## v0.1.2 — 2026-06-15

### 修复

- NVIDIA 显卡检测改为先查询 `nvidia-smi`，避免虚拟显示层干扰，并增加多路径兜底。
- 原生启动探测隔离到子进程（300 秒硬超时）：原生崩溃变为可处理的失败结果，启动器不再直接消失。
- NVIDIA 系统上已安装 `ggml-cuda.dll` 时，后端标签正确显示 `GGUF + CUDA`。

## v0.1.1 — 2026-06-02

### 新增

- NVIDIA CUDA 加速作为应用内一键升级（可选 ~595 MB 附加包，不增加主包体积）。
- `Start_CPU_only_仅CPU启动.bat` 便捷启动器，便于 CPU 与集显 A/B 对比。

### 变更

- 后端报告：`/api/system-info` 增加 `gguf_accelerator` 字段；新增 CUDA 升级状态与下载接口。

## v0.1.0 — 2026-05-26

### 新增

- Windows 10/11 x64 首个发行版：本地 TTS（Qwen3-TTS-12Hz-1.7B-CustomVoice，GGUF + Vulkan）与本地 STT（SenseVoice Small；可选 Parakeet TDT 英文专用模型）。
- 嵌入式 Python 3.12.10；约 108 MB 精简 ZIP，模型于首次启动按需下载（GGUF 2.2 GB / STT 242 MB）。
- 断点续传、3 次指数退避重试与中国大陆友好镜像；离线隐私姿态（无遥测、无自动更新探测）。
