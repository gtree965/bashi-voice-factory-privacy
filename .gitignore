# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.venv-*.txt
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
release_artifacts/
python-*-embed-amd64/
python-*-embed-amd64.zip

# Logs and temp
*.log
launch_log.txt
.tmp/
.stt_metrics/
tmp_*/
tmp*/
*.tmp
*.bak

# IDE / OS
.vscode/*
!.vscode/settings.json
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Secrets / local config
.env
.env.local
*.secret

# External reference material (not authored content)
*.odt
.~lock.*#

# Internal evaluation / review artifacts (kept local only)
总结报告_*.html
*_Comprehensive_Evaluation_Report.html
roadmap_review_report_by_*.md
*.srt

# Only the automated regression suite is tracked. The GGUF baseline fixtures
# and the manual probe harness stay local-only: they are large, machine
# specific, and carry sample text that must not ship in a public repository.
tests/*
!tests/test_*.py

# Generated audio, except product UI style previews.
static/audio/*
!static/audio/style_previews/
!static/audio/style_previews/**
static/uploads/

# Local model/data caches
models/
.backend_probe_cache.json
*.gguf
*.onnx
*.onnx.data
*.safetensors
*.pt
*.pth
*.bin

# Third-party voice reference material (downloaded, NOT ours).
# Test-only: usable for local clone/blind-test work, but carries third-party
# rights and must never be committed or shipped in a release package.
/data_aishell3.tgz
/data_aishell3/
800多克隆必备音色素材合集/
800多克隆必备音色素材合集.zip
