# Pinned runtime dependencies for Bashi Voice Factory Privacy Edition.
# If this file changes, rerun scripts/precheck_py312_embed.ps1 before release.

Flask==3.0.3
imageio-ffmpeg==0.5.0
numpy==1.26.4
soundfile==0.12.1
torch==2.10.0
transformers==4.57.3
accelerate==1.12.0
huggingface_hub==0.36.2
safetensors==0.7.0
# qwen-tts itself is installed with --no-deps by the portable launcher and
# embed precheck so its bare onnxruntime dependency cannot overwrite DirectML.
librosa==0.11.0
torchaudio==2.10.0
sox==1.5.0
einops==0.8.2
# gradio is only imported by qwen_tts.cli.demo; the app does not ship that UI.
sherpa-onnx==1.12.39
gguf==0.18.0
onnx==1.21.0
onnxruntime-directml==1.23.0
sentencepiece==0.2.1
sounddevice==0.5.5
